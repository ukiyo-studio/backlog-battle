# Supabase — client-safe (required for Phase 1)
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
# Expo push tokens (required for native reminders in Phase 4)
EXPO_PUBLIC_EAS_PROJECT_ID=
# Server-only — NOT needed for Phase 1 MVP; add later for Edge Functions/admin scripts only
# SUPABASE_SERVICE_ROLE_KEY=
# EXPO_ACCESS_TOKEN=  # Edge Function secret for process-reminders (server-only)