/// <reference types="nativewind/types" />
// Augments react-native component props with `className` so src/components/**
// typechecks standalone. Harmless duplicate if the app scaffold adds its own
// nativewind-env.d.ts at the repo root.
